const rq = require("../utils/makeReq.js")

export const User = {
    send: function() {
        if(!arguments[0]) return console.log("Cannot send empty message!")
        let messageData
        for (let i = 0; i < arguments.length; i++) {

        }
    }
}